"""
Top-level package for the data pipeline.

The package groups together a collection of scripts in `cj_scripts` that handle loading, transforming, and exporting geometry and attributes, centered on CityJSON.
Public entry points are exposed via a CLI defined in `cli.py`.
"""
